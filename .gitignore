node_modules
.next
out
__pycache__/
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# impeccable writes its per-repo hook state here
.impeccable/

# Scratch comparison builds. site/ is the published folder — anything left
# here is served from the live domain, so these must never be committed.
site/_cmp.html
